<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
   
    <table>
        <% if (foodkwlist.length > 0) { %>
            <%for (element of foodkwlist){ %>            
                <tr>
                    <td> <%=element.id%> </td>
                    <td> <a href="/getid/<%=element.id%>"> <%=element.name%> </a></td>           
                </tr>            
            <% } %>
        <% } else { %>
            <p>NO FOOD</p>
            <% } %>
    </table>
    <a href="/">retout à l'acceuil</a>
</body>
</html>